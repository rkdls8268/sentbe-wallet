server:
  port: 8080

spring:
  application:
    name: sentbe

  datasource:
    url: jdbc:postgresql://localhost:5432/sentbe
    username: testuser
    password: 1234
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: INFO
    com.sentbe: DEBUG